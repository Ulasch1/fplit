DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/fplit?schema=public"
PORT=4000
# Comma-separated allowed origins for CORS (the deployed frontend URL). Leave empty for local dev (allows all).
CORS_ORIGIN=
